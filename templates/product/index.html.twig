{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block title %}Nos produit-la boutique française{% endblock %}

{% block content %}
<div class="row">
    <div class="col-md-3">
        <h3>Rechercher</h3>
          {{form_start(form)}}
          {{form_row(form.searchByName)}}
          {{form_end(form)}}
    </div>
    <div class="col-md-9">
        <h1>Nos produits</h1>
        <div class="row">
            {% for product in products %}
            <div class="col-md-4">
                {% include 'product/single_product.html.twig' %}
            </div>
            {% endfor %}
            
        </div>
    </div>
</div>

{% endblock %}
